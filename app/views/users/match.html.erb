<h2>Your Match</h2>


<% if @match %>
<div class="container">
    
    <%= render 'users/user_match', foundmatch: @match, percent: @percent %>
    
    <% @user.answers.each do |test|  %>
    
        <div class="row">
            <div class="col-sm-12">
                <%= test.id %>
                Question:<%= test.question_id %>
                User:<%= test.user_id %>
                <%= test.ans %>
            </div>
        </div>
    <% end %>
</div>
<% elsif %>

<div class="container">
    <div class="panel panel-default">
        <div class="panel-body">
            <h4>Opps, it seems like you are a weird one.</h4>
            <p>Keep answering questions</p>
            
            
        </div>    
    </div>

</div>
<% end %>